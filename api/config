const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo */
    /* University Address : 10.13.130.15 */

    // host: "192.168.1.86",
    host: 'localhost',
    user: "root",
    password: "password",
    database: "project",
  }
};
module.exports = config;
